

<div class='container-fluid'>
  <% product_types.each(function(product_type, x){ %>
    <% if (x%6 === 0) { %>
       <!-- <row class='container-fluid'> -->
    <% } %>
        <div class="s-market-button rounded-corners market-button <%=product_type.escape('name')%> button-width"
           data-type="<%=product_type.escape('name')%>">
          <button class="go-to-market helper rounded-corners btn btn-block btn-<%=product_type.escape('name')%>">
           <span> <%=product_type.escape('name')%> </span>
          </button>
        </div>
    <% if ((x+1) % 6 === 0 && x != 0) { %>
       <!-- </row> -->
    <% } %>
  <% }) %>
</div>


    <row class='container-fluid'>
      <div class='col-xs-3 left-show'>
 	<ol>
 		<li>Choose what looks tastiest</li>
 		<li>Drag the farmer thumbnails from the index to the bottom bar for a closer look</li>
 		<li>Click your favorite farmers and visit their profile pages, <br>
 		where you can have a closer look at their products, and learn how to contact them</li>
 	</ol>
 	
	  </div>
      <div class='col-xs-6 show-area'>  </div>
      <div class='col-xs-3'></div>
    </row>




    <row class='container-fluid'>
      <div class='col-xs-3'></div>
      <div class='col-xs-6'>

        <div class='index-map-container'>
    <button class='btn btn-success ind-map-button add-map'>ADD MAP</button>
           <div class='map-canvas' id="index-map">
           </div>
        </div>

       </div>
      <div class='col-xs-3 down-arrow'></div>
    </row>

